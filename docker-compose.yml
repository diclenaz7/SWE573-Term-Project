services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: django_db
      POSTGRES_USER: django_user
      POSTGRES_PASSWORD: django_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432" # optional, for connecting from host (psql, DBeaver, etc.)

  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    develop:
      watch:
        - action: sync
          path: ./appsite
          target: /code/appsite
        - action: sync
          path: ./manage.py
          target: /code/manage.py
        - action: rebuild
          path: ./requirements.txt
        - action: sync
          path: .
          target: /code
          ignore:
            - db.sqlite3
            - __pycache__/
            - .git/
            - node_modules/
            - .venv/
            - venv/
            - templates/
  frontend:
    build:
      context: ./frontend
    command: npm start
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true # helps hot reload in Docker on macOS

volumes:
  postgres_data:
